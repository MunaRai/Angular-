import { NavLinksFilterPipe } from './nav-links-filter.pipe';

describe('NavLinksFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NavLinksFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
